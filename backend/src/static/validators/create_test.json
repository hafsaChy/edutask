{
    "$jsonSchema": {
        "bsonType": "object",
        "required": ["email"],
        "properties": {
            "email": {
                "bsonType": "string",
                "description": "the email address of a user must be determined and unique",
                "uniqueItems": true
            },
            "name": {
                "bsonType": "string",
                "description": "the name is optional and can be duplicate"
            },
            "task": {
                "bsonType": "string",
                "description": "the task is optional and can be duplicate"
            }
        }
    }
}
